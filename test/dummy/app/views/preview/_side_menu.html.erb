<div class="nav-side-menu">
  <div class="brand">Sidenav Header</div>
  <i class="fa fa-bars fa-2x toggle-btn" data-toggle="collapse" data-target="#menu-content"></i>
  <ul id="menu-content" class="nav in  navbar-collapse active">

    <% @elements[:children].each do |el| %>
      <% unless el[:children].nil? %>
        <li class="<%= @section.downcase.include?(el[:parent_name].downcase ) ? 'active' : '' %>">
          <a href="#"><i class="fa <%=el[:icon]%> fa-lg fa-fw"></i><%= el[:display_name] %><span class="fa arrow"></span></a>
          <%= render_children el[:children] %>
        </li>
      <% else %>
        <%=activatable_li_tag element_path(el[:parent_name]) do %>
          <% @is_active = (@section.downcase.include?(el[:parent_name].downcase)) ? 'active' : '' %>
          <%= link_to element_path(el[:parent_name]), class: @is_active do %>
            <i class="fa <%=el[:icon]%> fa-fw fa-lg"></i><%= el[:display_name].titleize %>
          <% end %>
        <% end %>
      <% end %>

    <% end %>

    <li>
      <a href="#"><i class="fa fa-sitemap fa-fw"></i> Multi-Level Dropdown<span class="fa arrow"></span></a>
      <ul class="nav nav-second-level collapse" aria-expanded="false">
        <li>
          <a href="#" class="active">Second Level Item</a>
        </li>
        <li>
          <a href="#" class="active">Second Level Item</a>
        </li>
        <li>
          <a href="#">Third Level <span class="fa arrow"></span></a>
          <ul class="nav nav-third-level">
            <li>
              <a href="#" class="active">Third Level Item</a>
            </li>
            <li>
              <a href="#" class="active">Third Level Item</a>
            </li>
            <li>
              <a href="#" class="active">Third Level Item</a>
            </li>
            <li>
              <a href="#" class="active">Third Level Item</a>
            </li>
          </ul>
          <!-- /.nav-third-level -->
        </li>
      </ul>
      <!-- /.nav-second-level -->
    </li>
    <li>
      <div class="nav-side-menu-footer">
        <div>
          <small>
            <strong>3 Users</strong> / 10 Users
            <span class="pull-right">
              <button class='btn btn-outline-info btn-flat btn-xs'><i class="fa fa-plus"></i></button>
            </span>
          </small>
        </div>
        <div class="progress progress-mini">
          <div class="progress-bar progress-bar-primary" role="progressbar" aria-valuenow="33" aria-valuemin="0" aria-valuemax="100" style="width: 33%"></div>
        </div>
        <div class='text-center'>
          100% operatinal capacity
        </div>
      </div>
    </li>
  </ul>
</div>